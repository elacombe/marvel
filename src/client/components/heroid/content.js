import React from 'react';

const Links = ({ arg1, arg2 }) => {

};

Links.propTypes = {
  arg1: React.PropTypes.array.isRequired,
  arg2: React.PropTypes.array.isRequired,
};

export default Links;
